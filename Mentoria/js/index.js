var nScroll = $(window).scrollTop();

/* Função de click no botao para Scroll para Inscrição */
function scrollWin(x, y) {
  window.scrollBy(x, y);
}
/* Função para Conferir se houve o Scroll */
$(document).scroll(function () {
  if ((nScroll) => 100) {
    
  }
});

/* Função pra Saber se o Scroll foi para Cima ou para Baixo */
$(window)
